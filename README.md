# JavaScript Array Example

This example updates the value at array index `2` to `go to temple`.

## Run

```bash
node demo.js
```

## Array update

```javascript
newYearsResolutions[2] = 'go to temple';
```

JavaScript arrays use zero-based indexing, so index `2` refers to the third item.
